server {
    listen 80;
    server_name localhost;

    location /images/ {
        root /src/www;
        autoindex on;
        gzip on;
        expires 1h;
        access_log /var/log/nginx/images.log;
    }
    
}